My first run, very happy with how it turned out.
I think claude is tripping a bit when reading e0 as random noise, its not, its the model after exactly one epoch and the modulus caused an early save.
This model is doing 100x better than I expected.

I'm going to move to minibatching and maybe try masking more of the image.