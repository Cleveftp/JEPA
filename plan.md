# JEPA Build Plan
This document describes how the JEPA model will be constructed.

## What I know
- Involves two encoders.
- Cutout augmentation vs ground truth encoding.
- MSE for latent space comparison.
- Training cycle is self-supervised.
- I want to use numba or cupy
- Implement custom neural network

## Steps
1. Figure out which, numba or cupy. (CuPy selected for direct mapping to VRAM)
2. Convolutional networks with CuPy.
3. Dual encoder pipeline.
4. Cutout sampling for second encoder.
5. Exponential moving average on weights (Somehow take the weights from the student model and update very slightly the teacher model)
6. Minimize MSE
7. Evaluate the generated representations on a classification task


I was wrong, JEPA doesnt work the way I thought it did, time to refactor.

It works by:
1. Getting a ground truth of patches generated by a teacher (each patch is a token from an embedding space).
2. Getting a student to do the same thing with an image of masked tokens.
3. Getting a predictor to semantically figure out why the tokens are different.

Realizations:
1. I need an embedding space. (done)
2. I need a transformer. (done)

## Steps
1. Get tokens from image using embedding space
1. Get teacher to generate patch embeddings
2. Get student to generate patch embeddings with tokens missing based on positional encoded remnants
3. Compare tokens with predictor and get loss
4. Predictor and student backpropagation
5. Teacher EMA
6. Profit



IT WORKS!!!!
